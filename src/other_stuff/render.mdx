import { Image } from "astro:assets";

export const imgSize = 200;
export const alt = "render";
export const href = "/api/images/render.png"

##### Render

No thanks I hate it.

<Image src={href} alt={alt} width={imgSize} height={imgSize} />

I genuinely dislike this image for a LOT of reasons.  It has the genAI "cutesy" brand that feels aggressively artificial.
import { Image } from "astro:assets";

export const imgSize = 200;
export const alt = "mixture";
export const href = "/api/images/mixture.png"

##### Combination

What happens when I ask the AI to be truly creative?

<Image src={href} alt={alt} width={imgSize} height={imgSize} />

I get the equivalent of a videogame cover-image.  This is an example that shows how AI truly is a form of art theft.  I am nearly certain that the castle in the leftmost panel of the background looks like one of the Hyrule castles from the Breath of the Wild series.
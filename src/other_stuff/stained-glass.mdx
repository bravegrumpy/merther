import { Image } from "astro:assets";

export const imgSize = 200;
export const alt = "stained glass";
export const href = "/api/images/stained-glass.png"

##### Stained Glass

<Image src={href} alt={alt} width={imgSize} height={imgSize} />

I actually do like this.  Part of it is becasue it actually used a color *other* than red as a predominant color.  I also think I'm drawn to the "transgressive" nature of gay stained glass.